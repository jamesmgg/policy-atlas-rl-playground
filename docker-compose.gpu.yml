# GPU training override:
#   docker compose -f docker-compose.yml -f docker-compose.gpu.yml up --build
services:
  rl-backend:
    environment:
      USE_GPU: "1"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
